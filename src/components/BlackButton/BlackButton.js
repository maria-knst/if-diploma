import './BlackButton.scss'

import React from 'react';

const BlackButton = ({ children }) => {
    return (
        <button>
            {children}
        </button>
    );
};

export default BlackButton;